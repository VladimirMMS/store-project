import { makeStyles } from '@mui/styles';

export const useStyle = makeStyles({
  container: {
    background: '#ADD8E6',
    height: '30vh',
    marginTop: '40px',
    textDecoration: 'none'
  },
  title: {
    paddingTop: '60px',
    fontFamily: 'serif',
    fontSize: '2em',
  }
});
