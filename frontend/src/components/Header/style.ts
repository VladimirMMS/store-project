import { makeStyles } from '@mui/styles';

export const useStyle = makeStyles({
  fontLetter: {
    fontFamily: 'Georgia, serif',
    fontSize: '25px',
    textDecoration: 'none',
    color: '#fff;'
  }
});
