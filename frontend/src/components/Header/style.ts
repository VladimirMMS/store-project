import { makeStyles } from '@mui/styles';

export const useStyle = makeStyles({
  fontLetter: {
    fontFamily: 'Georgia, serif',
    fontSize: '22px',
    textDecoration: 'none',
    color: '#fff;'
  },
  header: {
    width: '100%'
  }
});
